flask>=3.0.0
psycopg[binary,pool]>=3.1.18
# confluent-kafka  # removed: event bus is now Postgres LISTEN/NOTIFY (via healthcare-common)
requests>=2.31.0
PyJWT>=2.8.0
pyyaml>=6.0.1
# Local dep: pip install -e ../../libs/py-healthcare-common

# Shared runtime library (installed from GitHub for Render/prod builds;
# in local docker-compose dev this is superseded by the bind-mount).
healthcare-common @ git+https://github.com/healthcare-org-app/healthcare-common.git@main
